{
  "name": "backpackpi-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "clean": "rimraf dist || rd /s /q dist",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "csso": "^5.0.5",
    "esbuild": "^0.23.0",
    "html-minifier-terser": "^7.2.0",
    "rimraf": "^5.0.5"
  }
}
